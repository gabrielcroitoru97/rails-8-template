<%= form_with(model: favorite_place) do |form| %>
  <% if favorite_place.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(favorite_place.errors.count, "error") %> prohibited this favorite_place from being saved:</h2>

      <ul>
        <% favorite_place.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <%= form.hidden_field :place_id %>

  <div class="text-center">
    <%= form.label :note%>
    <%= form.text_field :note %>
    <%= form.submit "Save to Favorites"%>
  </div>
<% end %>
