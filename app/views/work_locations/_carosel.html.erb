<% if @work_location.images.any? %>

  <div class="d-flex flex-wrap justify-content-center gap-3 mt-3">
    <% @work_location.images.each do |image| %>
      <%= image_tag image.picture.url, class: "img-thumbnail", width: 200, loading: "lazy" %>
    <% end %>
  </div>
<% end %>
